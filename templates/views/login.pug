extends ../layouts/base
include ../mixins/_loginForm

block content
  h1.title Log In

  if (message)
    .notification
      =message
  br
  br
  div.box
    +loginForm
    small
      a(href="/forgot") Lost your password?
